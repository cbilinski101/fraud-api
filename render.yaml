services:
  - type: web
    name: fraud-predictor-enhanced
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run fraud_predictor_app.py --server.port 10000 --server.address 0.0.0.0

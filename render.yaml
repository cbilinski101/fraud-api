
services:
  - type: web
    name: fraud-predictor-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run fraud_predictor_app.py --server.port $PORT --server.address 0.0.0.0
